# Optional. If omitted, paste a key into Vibaocode Settings for the current browser session.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.3-codex

# Optional. Reads from public GitHub repos work without a token.
# Writes require a fine-grained GitHub token supplied from Settings.
